# development defaults — do NOT reuse them in production.

# --- Database ---
DB_NAME=habit_shaper
DB_USER=habit_user
DB_PASSWORD=habit_password
DB_ROOT_PASSWORD=rootpassword

# --- Backend ---
JWT_SECRET=dev_change_me

# --- Frontend (build-time) ---
VITE_API_URL=http://localhost:4000/api
